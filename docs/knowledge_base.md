Poner
- semantic release
- cómo funciona la creación de eventos offline, y como funciona la parte de multiprocessing
- que explique cómo se transforman los datos en el setup, como es el input del modelo
- arquitectura two towers con un esquema y cómo entran los datos al modelo (variables y sus tipos, cuales entran como embedding, cuales numéricas y en qué momento se normalizan)
- explicar onnx, qué es, qué es el onnx runtime, como se guardan los modelos, como se ejecuta en produccion
- como funciona flink, cómo le llega la información, qué hace transformaciones hace y donde y cómo la entrega
- como se hace el predict, cómo la arquitectura del two towers mejora la latencia, de donde se toman los datos, transformaciones, etc.
- como funciona el simulador online, temperatura, cómo funciona asyncio
